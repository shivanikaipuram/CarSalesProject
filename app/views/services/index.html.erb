
<h1>Listing Services</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @services.each do |ser| %>
      <tr>
        <td><%= ser.name %></td>
        <td><%= link_to 'Show', ser %></td>
        <td><%= link_to 'Edit', edit_service_path(ser) %></td>
        <td><%= link_to 'Destroy', car, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
